{
  "name": "crucible",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm --filter @crucible/cli --filter @crucible/adapters build",
    "test": "pnpm --filter @crucible/cli --filter @crucible/adapters test",
    "typecheck": "pnpm --filter @crucible/cli --filter @crucible/adapters typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "yaml": "^2.8.0"
  }
}
